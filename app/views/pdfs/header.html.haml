!!!
%html
  %head
    %meta{:charset => 'utf-8'}

    %style
      = Rails.application.assets.find_asset('application.css').to_s
      = Rails.application.assets.find_asset('bootstrap.css.scss').to_s
      = Rails.application.assets.find_asset('pdf.css').to_s

  %body
    .page-header
      %h1
        - case @target
        - when 'company'
          ΕΤΑΙΡΕΙΕΣ
          = "(#{@companies_length})" if @companies_length
        - when 'results'
          ΑΠΟΤΕΛΕΣΜΑΤΑ
          = "#{@company.short_name}" if @company
        - when 'invoices'
          ΕΠΙΣΚΟΠΗΣΗ ΤΙΜΟΛΟΓΙΩΝ
        - when 'assets'
          ΠΑΓΙΑ
          - if @company
            .pull-right
              .btn.btn-success
                = @company.name
                %br
                Α.Φ.Μ.
                = @company.afm
                Δ.Ο.Υ
                = @company.doy
          = "(#{number_format(@assets_value)} €)"
